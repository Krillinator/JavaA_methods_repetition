        *** Classes + object in main (
                Create class,
                to object,
                to within main
            )
        *** Split up methods
        *** Argument/Params
        *** keywords + methods (static, this)
                *** Git Clone **//
                *** Our .git? Or their .git!
                // what happened to branches //

        *** Methods/Scanner that repeat until: "YES" | "NO"
        *** Restart program if done() (question YES NO also)

        // 11:15 fortsätter vi, skriv förslag på vad ni vill att jag tar upp!
